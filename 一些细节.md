# 一些细节

## 分组（group by）

我们常常都会用到分组，但是分组有个很容易让人产生误会的点。

比如：

```mysql
SELECT salary
FROM employees
GROUP BY department_id;
```

返回了12行结果

<img src="[sql-language/返回12行.jpg at week13 · SaltyFishy/sql-language (github.com)](https://github.com/SaltyFishy/sql-language/blob/week13/返回12行.jpg)" alt="返回12行">

但是部门真的只有12行吗？



## 结束死循环

1.查看进程

```mysql
SHOW PROCESSLIST;
```

2.杀死对应进程

```mysql
KILL 线程名;
```

